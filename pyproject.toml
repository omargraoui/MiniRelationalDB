[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "minirelationaldb"
version = "0.1.0"
description = "An in-memory relational engine with a deliberately limited SQL-like subset"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
